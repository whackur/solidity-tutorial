{
  "name": "simple-wallet",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "npx hardhat test",
    "compile": "npx hardhat compile",
    "check-code": "npx hardhat run scripts/check-code.ts",
    "deploy:sepolia": "pnpm hardhat ignition deploy --network sepolia ignition/modules/SimpleWallet.ts --verify --reset",
    "lint:sol": "solhint contracts/**/*.sol"
  },
  "devDependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "hardhat": "^3.0.11",
    "viem": "^2.38.6"
  }
}
